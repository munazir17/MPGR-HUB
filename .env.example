NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=2e1123b09e786a59f1af6b27668fda6a
NEXT_PUBLIC_BASE_RPC_URL=https://mainnet.base.org

# --- Game Rewards Module (server-only) --------------------------------
# See docs/GAME_REWARDS_SETUP.md for the full one-time setup checklist.

# Vercel KV (Upstash Redis) — provisioned via the Vercel dashboard
# (Storage -> Create Database -> KV). These are populated automatically
# when the KV store is linked to this project; listed here only so local
# dev / other hosts know what's required.
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Private key of the wallet that will be granted rewardManager status on
# the deployed MPGRRewardVault (0xbe4B0e8692670229129562a50A62f5173E30937C,
# Base Mainnet). NEVER prefix this with NEXT_PUBLIC_. Generate a dedicated
# key for this purpose — do not reuse an existing hot wallet.
REWARD_MANAGER_PRIVATE_KEY=

# Optional server-only Base RPC URL (falls back to
# NEXT_PUBLIC_BASE_RPC_URL if unset). Use a private/authenticated RPC
# endpoint in production for reliability. Alchemy free-tier CU limits
# make Basename + quote reads flaky — prefer a paid Base key.
BASE_RPC_URL=

# Optional Ethereum Mainnet RPC used only as a Basename fallback
# (CCIP-Read). Not required now that Basenames resolve on Base L2 first.
MAINNET_RPC_URL=

# Gemini is the default agent provider. Free-tier gemini-3.5-flash is
# ~20 generateContent calls/day; after that the agent falls back to
# local FAQ replies. Use a billed Google AI Studio / Vertex key.
GEMINI_API_KEY=
GEMINI_MODEL=

# NVIDIA NIM is Gemini's first network fallback, then OpenAI, then the
# on-device engine. Server-only — never prefix with NEXT_PUBLIC_.
# Hosted OpenAI-compatible API: https://integrate.api.nvidia.com/v1
# Missing NVIDIA_API_KEY skips NVIDIA with 503 (does not break Gemini).
# Default model is nvidia/nemotron-3-super-120b-a12b (live catalog,
# tool-calling). llama-3.3-nemotron-super-49b-v1.5 is no longer hosted.
NVIDIA_API_KEY=
NVIDIA_MODEL=nvidia/nemotron-3-super-120b-a12b
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1

# Shared secret Vercel Cron sends as `Authorization: Bearer <value>` when
# calling /api/games/mpgr-run/settlement. Generate with e.g.
# `openssl rand -hex 32`. Required — the settlement route refuses to run
# without it configured.
CRON_SECRET=

# --- AI rate limits & daily cost caps (Redis, server-only) --------------
# Per-minute dual-bucket (per-IP + per-wallet) limiter is 20/min for /api/agent/complete*.
# Daily budgets share the same Redis (Vercel KV). When either per-wallet or global budget
# is exceeded the route returns a generic 429/503 that the client treats as a provider
# failure and falls back to the deterministic engine — UX stays available, cost is capped.
# All values are optional; safe defaults are used when unset. Fail closed if Redis is unreachable.
AI_DAILY_REQUESTS_PER_WALLET=100
AI_DAILY_REQUESTS_GLOBAL=5000
AI_DAILY_TOKENS_PER_WALLET=100000
AI_DAILY_TOKENS_GLOBAL=2000000

# --- Referral abuse hardening (Task 8; server-only) -------------------
# Maximum referral rewards (REFERRAL_SUCCESS, +100 XP) a single referrer
# may be credited per UTC day. Checked and consumed atomically in Redis
# when a pending referral reward settles. Referral ATTRIBUTION itself is
# never capped — every legitimate referred wallet keeps its credit; only
# payouts are throttled. A referral reward additionally requires:
#   1) the referred wallet to have genuine server-awarded activity
#      (a daily check-in), and
#   2) the referrer to already exist in the server XP ledger.
# Missing/invalid values fall back to the safe default (5). It can only
# be raised (max 1000), never turned off — this is fail-closed by design.
# Production currently runs with no override set, i.e. the default.
REFERRAL_REWARDS_PER_REFERRER_PER_DAY=5

# --- Authentication / security ---------------------------------------
# Server-only HMAC secret used to sign short-lived wallet sessions.
# Generate with: openssl rand -hex 32
AUTH_SESSION_SECRET=
# Canonical browser origin used in the wallet sign-in message and CSRF Origin check.
# Required in production (VERCEL_ENV=production). Example: https://mpgrhub.xyz
# Vercel Preview (VERCEL_ENV=preview) may omit this — the request origin is used
# so same-origin POSTs still verify. Do not disable origin verification.
# Example: https://mpgr-hub.example
APP_ORIGIN=

# Real-value game rewards remain disabled until authoritative game
# verification is reviewed. The authoritative verifier is the in-process
# deterministic replay (lib/games/mpgr-run/authoritative-replay.ts) —
# no external GAME_RUN_VERIFIER_URL/SECRET is required.
GAME_REWARDS_ENABLED=false
GAME_AUTHORITATIVE_VERIFICATION_ENABLED=false

# --- Base Stocks live tape (/agent) ------------------------------------
# Cache lifetime (seconds) of the server-aggregated tape snapshot served
# by GET /api/market/tape, GET /api/market/pair and the paid
# GET /api/x402/tape. Clamped to 5-15s; default 10.
TAPE_CACHE_TTL_SECONDS=10
# A Coinbase equity Chainlink feed freezes off-hours by design (0.5%
# deviation / 24h heartbeat while markets are open). Feed answers older
# than this are flagged stale:true on the tape (last value still shown).
TAPE_FEED_STALE_SECONDS=93600

# --- x402 paid tape endpoint (GET /api/x402/tape) ----------------------
# Price of one live tape snapshot in atomic USDC (6 decimals).
# 20000 = $0.02. Values <= 0, non-integer, or above 1 USDC fall back
# to the 20000 default.
X402_TAPE_PRICE_USDC_RAW=20000
# Base address that receives tape payments. REQUIRED to enable the
# endpoint — unset/invalid makes /api/x402/tape fail closed with 503
# (it never serves the tape for free).
X402_TAPE_PAY_TO=
# Optional facilitator override (https only). Default: the CDP
# facilitator (https://api.cdp.coinbase.com/platform/v2/x402) when
# CDP_API_KEY_ID/CDP_API_KEY_SECRET are set, otherwise the public
# https://x402.org/facilitator (Base Sepolia only — mainnet settles
# fail closed there until CDP keys or an override are configured).
X402_TAPE_FACILITATOR_URL=
